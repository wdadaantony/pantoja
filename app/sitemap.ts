import type { MetadataRoute } from 'next';import { repuestos,vehiculos } from '@/lib/data';
export default function sitemap():MetadataRoute.Sitemap{const base=process.env.NEXT_PUBLIC_SITE_URL||'http://localhost:3000';const pages=['','/vehiculos','/repuestos','/nosotros','/contacto'].map(path=>({url:`${base}${path}`,lastModified:new Date(),changeFrequency:path?'weekly' as const:'daily' as const,priority:path?0.8:1}));return [...pages,...vehiculos.map(v=>({url:`${base}/vehiculos/${v.slug}`,lastModified:new Date(),changeFrequency:'weekly' as const,priority:.8})),...repuestos.map(p=>({url:`${base}/repuestos/${p.slug}`,lastModified:new Date(),changeFrequency:'weekly' as const,priority:.7}))]}
