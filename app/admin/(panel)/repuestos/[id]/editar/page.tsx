import { notFound } from 'next/navigation';import { AdminTopbar } from '@/components/AdminTopbar';import { PartForm } from '@/components/PartForm';import { repuestos } from '@/lib/data';export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params;const p=repuestos.find(x=>x.id===id);if(!p)notFound();return <><AdminTopbar title={`Editar ${p.nombre}`} subtitle="Actualiza la información del repuesto"/><div className="admin-content form-content"><PartForm part={p}/></div></>}
